# Python
__pycache__/
*.pyc

# Virtual environments
.venv/
.vnv/

# Model checkpoints
*.pt
*.pth

# Experiment results, run logs, TensorBoard logs (regenerated by training runs;
# keep outside Git so push/pull don't carry them between machines)
outputs/

# VS Code
.vscode/

# OS files
.DS_Store

# Datasets (keep datasets outside Git; symlink via scripts/setup_data_links.sh)
data/