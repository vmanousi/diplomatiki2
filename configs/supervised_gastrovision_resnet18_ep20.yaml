experiment_name: exp05_supervised_gastrovision_resnet18_ep20

seed: 42

dataset:
  name: gastrovision_webdataset
  root: data/Gastrovision_webdataset
  image_size: 224
  num_classes: 22

model:
  name: resnet18
  pretrained: false

transforms:
  name: supervised_basic
  image_size: 224

training:
  epochs: 20
  batch_size: 8
  learning_rate: 0.0001
  num_workers: 2
